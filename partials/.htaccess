# This HTML fragment is only ever pulled in by index.php via PHP include,
# never loaded directly by the browser. Block direct web access.

<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
